import AuthCard from "./AuthCard/AuthCard";

export default function LoginCadastro() {
  return (
    // Não precisamos de div extra ou styles aqui,
    // pois o AuthCard já tem o wrapper de tela cheia (pageWrapper)
    <AuthCard />
  );
}
